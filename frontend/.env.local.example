# Copy to .env.local. The frontend reads only NEXT_PUBLIC_* at build/runtime.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Dev-only convenience for the Settings page (admin API). In production the admin
# surface would sit behind SSO, not a public env var.
NEXT_PUBLIC_ADMIN_TOKEN=dev-admin-token
